/**
 * ratio - the aspect ratio.
 * offsetLeft - the offset from the left side, ie: x + offsetLeft will give the correct position
 * offsetTop - the offset from the top, ie: y + offsetTop will give the correct position
 */
type Aspect = {
  ratio: number;
  offsetLeft: number;
  offsetTop: number;
}

export default Aspect;
